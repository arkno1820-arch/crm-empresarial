---
# Recarga (SIGHUP) en lugar de reiniciar: un cambio de configuracion o de pg_hba no debe
# provocar una conmutacion innecesaria del primario.
- name: Reiniciar Patroni
  ansible.builtin.shell: systemctl daemon-reload && (systemctl reload patroni || true)
  changed_when: false
